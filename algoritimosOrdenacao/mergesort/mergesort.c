#include <stdio.h>
#include <stdlib.h>

void merge(int *vetor, int inicio, int meio, int fim){
    int i = inicio;
    int j = meio + 1;
    int k = 0;
    int *aux = malloc(sizeof(int)) * (fim - inicio + 1);

    
}

int main(){
    return 0;
}